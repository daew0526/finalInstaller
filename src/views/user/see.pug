extends ../base
block content
    div.user
        if user.socialOnly
            img(src=user.avatarUrl,style="width:100px")
        else
            img(src=user.avatarUrl,style="width:100px")
        h3=user.username
        h3=user.email